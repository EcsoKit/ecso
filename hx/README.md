# ecso

```haxe
typedef User = {
    var age : Int;
    var xp : Int;
}

typedef UserReader = {
    var description : String;
}
 
typedef Pos = { x:Int, y:Int, fixed:Bool }
typedef Target = { target:Pos }
```